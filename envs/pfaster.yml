# Mirrors pfaster/environment.yml from the PfaSTer repo
name: pfaster
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.7
  - numpy
  - pandas
  - scikit-learn
  - biopython
